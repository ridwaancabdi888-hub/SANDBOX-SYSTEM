/** Shared by the manifest and the root layout's viewport `themeColor`. */
export const PWA_THEME_COLOR = "#d95a0f"; // --brand-600

/** The logo's own background, so the splash screen blends into the icon. */
export const PWA_BACKGROUND_COLOR = "#fdf7ee";

export const SERVICE_WORKER_URL = "/sw.js";
